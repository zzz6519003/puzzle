$('document').ready(function(){
	// if($('#swfupload')){
// 		
		// var settings = {
		// upload_url:"/addPicture",
		// flash_url:"/static/swfupload/vendor/swfupload/swfupload.swf",
		// button_placeholder_id: "button",
		// file_size_limit:"20480",
		// button_width:200,
		// button_height:20,
		// button_text:'点击选择',
		// debug:true
		// };
		// $('#swfupload-control').swfupload(settings);
// 	
    	// // assign our event handlers
    	// $('#swfupload-control')
        	// .bind('fileQueued', function(event, file){
            	// // start the upload once a file is queued
            	// $(this).swfupload('startUpload');
            	// console.log("startload");
        	// })
        	// .bind('uploadComplete', function(event, file){
            	// console.log('Upload completed - '+file.name+'!');
            	// // start the upload (if more queued) once an upload is complete
            	// $(this).swfupload('startUpload');
        	// });
	// }
	
        
    // if ($('#tab td')) {
//     	
    	// $('#tab td').popover();
//     	
    // }
});
